{
  "mcpServers": {
    "requirements": {
      "type": "stdio",
      "command": "python",
      "args": ["-m", "mcp_tools.requirements_manager"],
      "env": {
        "PYTHONPATH": "${PWD}"
      }
    }
  }
}
