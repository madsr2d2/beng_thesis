::: {.landscape-tables}
| ID | ISO ref | Priority | Requirement | Notes |
| :--- | :------------ | :-------- | :-------------------------------------------------------- | :--------------- |
| [REQ-001]{#req-001 .req-id} | 6.4.5.2 | P1 | **LLC notification** `\newline`{=latex} LLC shall notify the user on every successful DF/RF TX or RX. | Timestamping (§6.5.7) is optional per ISO and is out of scope. |
| [REQ-002]{#req-002 .req-id} | 6.4.5.5.2, 6.4.5.5.3 | P2 | **LLC transmission request and abort timing** `\newline`{=latex} 1) A TX request shall be processed no later than the second SOF after the request when no EFs are present. `\newline`{=latex} 2) Frames not yet passed to the MAC are aborted immediately. `\newline`{=latex} 3) An abort request shall be processed before the second SOF (no EFs present) or third SOF (with EFs present). | Frames already passed to MAC are aborted only on MAC error or arbitration loss. |
| [REQ-003]{#req-003 .req-id} | 6.5.2, 6.5.3 | P1 | **Retransmission** `\newline`{=latex} 1) Frames losing arbitration shall be retransmitted unless aborted by the LLC user. `\newline`{=latex} 2) Non-XL nodes shall support unlimited retransmissions. |  |
| [REQ-004]{#req-004 .req-id} | 6.5.4 | P3 | **Frame acceptance filtering** `\newline`{=latex} 1) Receivers should use acceptance filtering to decide frame relevance. `\newline`{=latex} 2) Filtering should be a single self-contained operation with no state carry-over from previous frames. |  |
| [REQ-005]{#req-005 .req-id} | 6.5.6 | P1 | **LLC transfer status reporting** `\newline`{=latex} LLC shall report five transfer status values: Ongoing, Transmitted, Lost_Arbitration, Disturbed, and Aborted, each reflecting the corresponding MAC sub-layer event. |  |
| [REQ-006]{#req-006 .req-id} | 6.6.4.4 | P1 | **CRC rules** `\newline`{=latex} 1) CRC_15 for CC frames. CRC_17 for FD frames with payload ≤16 B. CRC_21 for FD frames with payload >16 B. `\newline`{=latex} 2) Init vector: all-zero for CRC_15. MSB-one for CRC_17 and CRC_21. |  |
| [REQ-007]{#req-007 .req-id} | 6.6.5.2, 6.6.5.3, 6.6.6.2, 6.6.6.3 | P1 | **Error and overload flags** `\newline`{=latex} 1) Active error and overload flags: 6 consecutive dominant bits each. `\newline`{=latex} 2) Passive error flag: 6 consecutive recessive bits (may be overwritten by dominant bits from other nodes). `\newline`{=latex} 3) Error and overload delimiters: 8 recessive bits each. `\newline`{=latex} 4) After the flag, all nodes monitor the bus and simultaneously begin the remaining 7-bit delimiter on detecting the first recessive bit. |  |
| [REQ-008]{#req-008 .req-id} | 6.6.7.1, 6.6.7.2, 6.6.7.3, 6.6.7.4 | P1 | **Inter-frame space** `\newline`{=latex} 1) Intermission shall be exactly 3 recessive bits. `\newline`{=latex} 2) No DF or RF may be started during intermission. `\newline`{=latex} 3) EFs and OFs shall not be preceded by inter-frame space. `\newline`{=latex} 4) Bus idle is recognised at the third recessive intermission bit (error-active nodes/receivers), the eighth recessive suspend bit (error-passive transmitters), or on leaving bus-integration state. `\newline`{=latex} 5) An error-passive transmitter shall suspend for 8 bit times after intermission. If another node transmits during the suspend window, the node shall become a receiver. |  |
| [REQ-009]{#req-009 .req-id} | 6.6.7.5 | P1 | **Bus re-integration** `\newline`{=latex} 1) Nodes shall start bus re-integration on protocol startup or bus-off recovery. `\newline`{=latex} 2) Re-integration completes after 11 consecutive recessive bits at the sample point. `\newline`{=latex} 3) For FD-capable nodes, any synchronisation-causing edge shall reset the 11-bit recessive-bit counter. |  |
| [REQ-010]{#req-010 .req-id} | 6.6.7.3, 6.6.8 | P1 | **Frame initiation** `\newline`{=latex} 1) SOF shall be a single dominant bit. `\newline`{=latex} 2) A node shall send SOF only when the bus is idle. `\newline`{=latex} 3) A dominant third intermission bit causes a node with a pending frame (if error-active or previous receiver) to skip SOF and begin arbitration immediately. `\newline`{=latex} 4) A dominant bit detected during bus idle shall be interpreted as SOF. |  |
| [REQ-011]{#req-011 .req-id} | 6.6.10.1, 6.6.10.4 | P2 | **Remote frame** `\newline`{=latex} 1) RTR bit shall be dominant for DFs and recessive for RFs. `\newline`{=latex} 2) RFs have no data field. DLC indicates the requested data length. |  |
| [REQ-012]{#req-012 .req-id} | 6.6.10.2, 6.6.11.2 | P1 | **SRR and RRS reserved bits** `\newline`{=latex} 1) SRR (CE, FE): transmitted recessive. Receivers accept either polarity without form error. `\newline`{=latex} 2) RRS (FB, FE): transmitted dominant. Receivers accept either polarity without form error. |  |
| [REQ-013]{#req-013 .req-id} | 6.6.10.5, 6.6.11.5 | P1 | **CRC bit stream coverage** `\newline`{=latex} 1) CC: SOF, arbitration, control, and data fields (dynamic stuff bits excluded). `\newline`{=latex} 2) FD: SOF, arbitration, control, data, dynamic stuff bits, and stuff count field (fixed stuff bits excluded). |  |
| [REQ-014]{#req-014 .req-id} | 6.6.10.6, 6.6.11.6, 6.6.14 | P1 | **Frame acknowledgement** `\newline`{=latex} 1) Receivers shall ACK consistent frames and flag inconsistent frames with an EF. `\newline`{=latex} 2) Transmitters shall flag unacknowledged frames with an EF. `\newline`{=latex} 3) FD frames tolerate up to a 2-bit dominant ACK overlap to compensate for receiver phase differences. `\newline`{=latex} 4) The ACK delimiter shall be one recessive bit. `\newline`{=latex} 5) ACK shall not depend on the frame identifier. |  |
| [REQ-015]{#req-015 .req-id} | 6.6.10.3, 6.6.11.2, 6.6.11.3 | P1 | **CC/FD format differentiation** `\newline`{=latex} 1) In FD frames the FDF bit is recessive. The overlapping CC bit position (r0 in CBFF, r1 in CEFF) shall be dominant. `\newline`{=latex} 2) IDE is in the control field for CB/FB frames, and in the arbitration field for CE/FE frames. `\newline`{=latex} 3) The res bit (FB/FE) shall be dominant. |  |
| [REQ-016]{#req-016 .req-id} | 6.6.11.3 | P2 | **ESI bit transmission** `\newline`{=latex} 1) Recessive when the LLC ESI flag is set. `\newline`{=latex} 2) Dominant when LLC ESI flag is clear and the node is error-active. `\newline`{=latex} 3) Recessive when LLC ESI flag is clear and the node is error-passive. |  |
| [REQ-017]{#req-017 .req-id} | 6.6.11.5 | P1 | **Stuff bit count (SBC) field** `\newline`{=latex} 1) SBC: 3-bit Gray-coded count of dynamic stuff bits (0-7) plus parity bit, placed before the CRC field. `\newline`{=latex} 2) TX shall transmit its dynamic stuff-bit count (up to the first FSB) as the SBC field. `\newline`{=latex} 3) RX shall verify the received SBC against its own count. |  |
| [REQ-018]{#req-018 .req-id} | 6.6.10.5, 6.6.11.5 | P1 | **CRC delimiter** `\newline`{=latex} 1) CC frames: exactly one recessive CRC delimiter bit. `\newline`{=latex} 2) FD frames: transmitter sends one recessive bit. A second recessive bit is tolerated before the ACK slot. |  |
| [REQ-019]{#req-019 .req-id} | 6.6.13.1, 6.6.13.2, 6.6.13.3.1 | P1 | **Bit stuffing** `\newline`{=latex} 1) After 5 consecutive identical bits, insert a complementary dynamic stuff bit. RX discards it. `\newline`{=latex} 2) CC: dynamic stuffing spans SOF to FCRC. FD: SOF to end of data field. ACK and EOF are not stuffed. `\newline`{=latex} 3) FD CRC field: a fixed stuff bit precedes the SBC field and follows every fourth CRC bit, each the inverse of the preceding bit. No double stuffing. `\newline`{=latex} 4) RX discards FSBs and flags a form error if an FSB matches the preceding bit. |  |
| [REQ-020]{#req-020 .req-id} | 6.6.10.7, 6.6.11.7, 6.6.15.2 | P1 | **End of frame (EOF)** `\newline`{=latex} 1) EOF shall consist of exactly 7 recessive bits. `\newline`{=latex} 2) A receiver shall validate a frame after the sixth EOF bit. `\newline`{=latex} 3) A transmitter requires all seven EOF bits to be error-free. |  |
| [REQ-021]{#req-021 .req-id} | 6.6.10.2, 6.6.11.2, 6.6.17.4, 6.6.17.5 | P1 | **Bus arbitration** `\newline`{=latex} 1) Recessive-sent, dominant-monitored: lose arbitration and become a receiver. `\newline`{=latex} 2) Arbitration field: CB: base ID + RTR. CE: base ID + SRR + IDE + extended ID + RTR. FB: base ID + RRS. FE: base ID + SRR + IDE + extended ID. `\newline`{=latex} 3) CB wins over FB: CBFF sends dominant r0 where FBFF sends recessive FDF. `\newline`{=latex} 4) CB/FB wins over CE/FE: CEFF/FEFF sends recessive SRR where CBFF/FBFF sends dominant. `\newline`{=latex} 5) (CB, CE only) DF wins over RF with same ID via its dominant RTR bit. |  |
| [REQ-022]{#req-022 .req-id} | 6.6.21.2 | P1 | **MAC error detection** `\newline`{=latex} 1) Bit error: sent/monitored mismatch. Exceptions: recessive non-stuff bit during arbitration, recessive bit in ACK slot, dominant bit while sending a passive error flag. `\newline`{=latex} 2) Stuff error: sixth consecutive identical bit in a dynamically-stuffed field. `\newline`{=latex} 3) Form error: unexpected fixed-form bit value, or FSB not inverse of preceding bit (FB/FE). Exception: dominant last EOF bit or last delimiter bit is not a form error. `\newline`{=latex} 4) CRC error: CRC mismatch. FD also requires SBC match and correct parity. `\newline`{=latex} 5) ACK error: transmitter detects no dominant bit in the ACK slot. | Sub-claim 1 (bit error) is simulation-verified via recessive injection in test_bus_off (can_mac_pcs_fce_tb): the bus is forced recessive at frame start, causing a bit error on the first dominant drive. Sub-claims 2 (stuff error), 3 (form error), 4 (CRC error), and 5 (ACK error) each require a frame-aware stimulus source. Stuff error must target a stuff bit; form error must target a fixed-format field; CRC error must target a non-fixed-form bit before the CRC field (an injection landing on a fixed-form bit would trigger a form error instead, not a CRC error); ACK error must target the ACK slot. Closing sub-claims 2, 3, 4, and 5 requires either the Everllence-internal CAN FD reference model with targeted error injection capability, or white-box FSM state signals exposed from can_mac_fsm. All four outstanding sub-claims are covered by code inspection only. |
| [REQ-023]{#req-023 .req-id} | 6.6.21.3.1 | P1 | **Error flag initiation and CRC error behaviour** `\newline`{=latex} 1) On any detected error, start an EF at the next bit and inform the LLC. `\newline`{=latex} 2) Data-phase errors: switch to nominal bit rate before starting the EF. `\newline`{=latex} 3) CRC error: receiver withholds ACK and sends an EF at the first EOF bit (CC) or 3 bit-times after the CRC delimiter (FD). |  |
| [REQ-024]{#req-024 .req-id} | 6.6.21.3.2 | P1 | **Overload frame** `\newline`{=latex} 1) LLC-requested OF: optional, starts at the first intermission bit, at most 2 per inter-frame space. `\newline`{=latex} 2) Reactive OF: triggered by a dominant bit at the first two intermission bits, the last EOF bit (receivers only), or the last error/overload delimiter bit. Starts one bit after the trigger. |  |
| [REQ-025]{#req-025 .req-id} | 7.3.2, 7.3.3 | P1 | **PCS bit timing configuration** `\newline`{=latex} 1) Separate nominal and FD data bit rate configurations, each based on a programmable integer prescaler. `\newline`{=latex} 2) Sample point at end of Phase_Seg1. Sync_Seg = 1 tq. `\newline`{=latex} 3) IPT ≤ 2 tq. | Deployment constraints on the system (not RTL behaviors): prop_seg shall be programmed ≥ t_node(A) + t_node(B) + 2 × t_busline (§7.3.2). SJW shall not exceed min(Phase_Seg1, Phase_Seg2) in either phase (§7.3.2). FD data phase: Phase_Seg2 shall be programmed ≥ SJW and ≥ maximum IPT (§7.3.3). Oscillator tolerance df shall satisfy df < SJW / (20 × t_bit) and df < min(Phase_Seg1, Phase_Seg2) / (2 × (13 × t_bit − Phase_Seg2)) (§7.3.6). |
| [REQ-026]{#req-026 .req-id} | 7.3.4, 6.6.21.3.1 | P1 | **Transmitter delay compensation** `\newline`{=latex} 1) TDC applies in the FD data phase when BRS is recessive. Enable and prescaler (1 or 2) are programmable. `\newline`{=latex} 2) SSP position = measured transmitter delay + configured offset. `\newline`{=latex} 3) TDC active: bit errors at the SP are ignored. SSP errors are flagged at the following SP. |  |
| [REQ-027]{#req-027 .req-id} | 7.3.5.1, 7.3.5.2, 7.3.5.3, 7.3.5.4 | P1 | **PCS synchronisation** `\newline`{=latex} 1) One synchronisation per bit time, re-enabled after the next recessive SP. `\newline`{=latex} 2) Edge qualifies only if the previous SP was recessive and no positive phase error exists while sending dominant. `\newline`{=latex} 3) Hard sync: IFS edges (except first intermission bit), bus-integration, and FDF-to-res transition. Restarts bit time with Sync_Seg completed, unconstrained by SJW. `\newline`{=latex} 4) All other qualifying edges cause resync, except for the transmitter during the FD data phase. Positive error: Phase_Seg1 += SJW. Negative error: Phase_Seg2 -= SJW. Error ≤ SJW: same effect as hard sync. | RTL is stricter than ISO: mac_i.transmitting suppresses all sync unconditionally (line 177), not just resync on positive phase error. Safe since the transmitter is the timing source. |
| [REQ-028]{#req-028 .req-id} | 6.6.7.5, 7.4.2.2, 7.4.2.3, 8.1.4.4 | P1 | **PCS bus-off isolation and signalling** `\newline`{=latex} 1) During bus-off, the PCS shall not drive dominant bits. `\newline`{=latex} 2) PCS enters bus-off on a supervisor request and exits on a release request. |  |
| [REQ-029]{#req-029 .req-id} | 8.1.3.2, Table 14, Table 15 | P1 | **FCE reset and response** `\newline`{=latex} 1) TEC and REC shall both be reset to zero when the FCE enters its initial state. |  |
| [REQ-030]{#req-030 .req-id} | 8.1.4.2 rule a), rule b), rule c), rule d), rule e), rule f), rule g), rule h) | P1 | **Error counter rules** `\newline`{=latex} 1) REC +1 on any receiver-detected error (except bit errors during error/overload flag). `\newline`{=latex} 2) REC +8: first dominant bit after sending an error flag, or bit error while sending an active error/overload flag. `\newline`{=latex} 3) REC -1 on successful RX if 1-127. Stays 0 if already 0. Set to 119-127 if above 127. `\newline`{=latex} 4) TEC +8 when sending an error flag. Exceptions: error-passive ACK error, pre-arbitration stuff error. `\newline`{=latex} 5) TEC +8 on bit error while sending an active error or overload flag. `\newline`{=latex} 6) TEC -1 on successful TX (floor 0). `\newline`{=latex} 7) After 14 consecutive dominant bits post active error/overload flag (8 post passive error flag): both +8. Each additional 8 dominant bits: both +8. |  |
| [REQ-031]{#req-031 .req-id} | 8.1.4.3, 8.1.4.4, 8.1.3.2 | P1 | **Error confinement state transitions** `\newline`{=latex} 1) Error-passive when either error counter > 127. Error-active restored when both ≤ 127. `\newline`{=latex} 2) TEC > 255 causes bus-off. `\newline`{=latex} 3) Bus-off recovery requires 128 idle conditions. Both counters reset to zero on recovery. `\newline`{=latex} 4) FCE shall notify the LLC on entering bus-off state. | Sub-claim 4: bus-off recovery requires 128 separate idle detections (128 × 11 recessive bits), not 128 consecutive recessive bits. |
| [REQ-032]{#req-032 .req-id} | 6.6.11.3 | P1 | **BRS bit rate switching** `\newline`{=latex} 1) Recessive BRS: switch to FD data rate at the BRS sample point. Dominant BRS: keep nominal rate. `\newline`{=latex} 2) Rate switches back to nominal at the first CRC delimiter sample point. |  |
| [REQ-033]{#req-033 .req-id} | 6.4.3, 6.4.4, Table 5 | P1 | **DLC byte-count mapping** `\newline`{=latex} DLC 0-8: linear mapping. FD: DLC 9→12, 10→16, 11→20, 12→24, 13→32, 14→48, 15→64 bytes. CC: DLC 9-15 clamped to 8 bytes. |  |
| [REQ-034]{#req-034 .req-id} | 6.6.16 | P1 | **Byte/Bit transmission order** `\newline`{=latex} Within the data field byte 0 is sent first. Within each byte bit(7) is sent first. |  |
| [REQ-035]{#req-035 .req-id} | 8.1.5 | P1 | **Node start-up** `\newline`{=latex} 1) A node shall complete bus re-integration (11 consecutive recessive bits) before transmitting. `\newline`{=latex} 2) A single node receiving no ACK becomes error-passive but never bus-off. | Sub-claim 2 (lone node stays error-passive, never bus-off) is verified by test_rule_c_exception in can_fce_tb via the passive_tx_ack_error_exempt_1 Exception 1 mechanism. Sub-claim 1 (bus re-integration before TX) is verified by waveform inspection of normal_test and test_bus_off in can_llc_mac_pcs_fce_tb: the node is observed completing the 11-consecutive-recessive-bit reintegration phase before any transmission. |
| [REQ-036]{#req-036 .req-id} | 6.6.18 | P2 | **MAC data consistency** `\newline`{=latex} Frames shall be fully buffered before TX starts, or the LLC shall detect and invalidate frames corrupted mid-transmission. | Verify by code inspection which method the implementation uses and confirm it is applied consistently. If method (a) - confirm the MAC FSM only begins serialisation after the full frame is loaded. If method (b) - confirm the LLC has an error-detection path and can abort or invalidate an in-progress frame. |
| [REQ-037]{#req-037 .req-id} | 6.6.21.1, 6.6.21.4 | P2 | **Error signalling enable** `\newline`{=latex} A configuration parameter shall exist to enable or disable error signalling. |  |
| [REQ-038]{#req-038 .req-id} | 6.4.4 | P3 | **DLC padding** `\newline`{=latex} An LLC restricted to fewer bytes shall replace excess bytes with 0xCC padding in both TX and RX directions. | Only applicable if the implementation exposes a configurable maximum-data-byte restriction. If not implemented, waive. |

: ISO 11898-1 extracted requirements. {#tbl:appendix-requirements}
:::
