::: {.landscape-tables}
| ID | ISO ref | Priority | Requirement |
| :----- | :------------- | :-------- | :---------------------------------------------------------------------- |
| REQ-001 | 6.4.5.2, 6.5.7, 6.6.3 | P1 | **LLC notification and timestamping** `\newline`{=latex} 1) LLC shall notify the user on every successful DF/RF TX or RX. `\newline`{=latex} 2) MAC shall signal time reference points at the SOF sample point and the frame-valid sample point. `\newline`{=latex} 3) (P3) If timestamping is absent, all timestamp outputs shall be constant zero. |
| REQ-002 | 6.4.5.5.2, 6.4.5.5.3 | P2 | **LLC transmission request and abort timing** `\newline`{=latex} 1) A TX request shall be processed no later than the second SOF after the request when no EFs are present. `\newline`{=latex} 2) Frames not yet passed to the MAC are aborted immediately. `\newline`{=latex} 3) Frames already at the MAC are only aborted on MAC error or arbitration loss. `\newline`{=latex} 4) An abort request shall be processed before the second SOF (no EFs) or third SOF (with EFs). |
| REQ-003 | 6.5.2, 6.5.3 | P1 | **Retransmission** `\newline`{=latex} 1) Frames losing arbitration shall be retransmitted up to the configured limit unless aborted. `\newline`{=latex} 2) Non-XL nodes need not implement a retransmission counter but shall support unlimited retransmissions. `\newline`{=latex} 3) If implemented, the retransmission counter shall reset to zero on successful TX or on a new LLC TX request. |
| REQ-004 | 6.5.4 | P3 | **Frame acceptance filtering** `\newline`{=latex} 1) Receivers should use acceptance filtering to decide frame relevance. `\newline`{=latex} 2) Filtering shall be a single self-contained operation with no state carry-over from previous frames. |
| REQ-005 | 6.5.6 | P1 | **LLC transfer status reporting** `\newline`{=latex} LLC shall report five transfer status values: Ongoing, Transmitted, Lost_Arbitration, Disturbed, and Aborted, each reflecting the corresponding MAC sub-layer event. |
| REQ-006 | 6.6.4.4 | P1 | **CRC rules** `\newline`{=latex} 1) CRC_15 for CC frames. CRC_17 for FD frames with payload ≤16 B. CRC_21 for FD frames with payload >16 B. `\newline`{=latex} 2) Init vector: all-zero for CRC_15. MSB-one for CRC_17 and CRC_21. |
| REQ-007 | 6.6.5.2, 6.6.5.3, 6.6.6.2, 6.6.6.3 | P1 | **Error and overload flags** `\newline`{=latex} 1) Active error and overload flags: 6 consecutive dominant bits each. `\newline`{=latex} 2) Passive error flag: 6 consecutive recessive bits (may be overwritten by dominant bits from other nodes). `\newline`{=latex} 3) Error and overload delimiters: 8 recessive bits each. `\newline`{=latex} 4) After the flag, all nodes monitor the bus and simultaneously begin their 7-bit delimiter on detecting the first recessive bit. |
| REQ-008 | 6.6.7.1, 6.6.7.2, 6.6.7.3, 6.6.7.4 | P1 | **Inter-frame space** `\newline`{=latex} 1) Intermission shall be exactly 3 recessive bits. `\newline`{=latex} 2) No DF or RF may be started during intermission. `\newline`{=latex} 3) A dominant third intermission bit shall be interpreted as SOF. `\newline`{=latex} 4) EFs and OFs shall not be preceded by inter-frame space. Consecutive OFs shall not be separated by inter-frame space. `\newline`{=latex} 5) A pending frame shall be started at the first bit following intermission. `\newline`{=latex} 6) Bus idle is recognised at the third recessive intermission bit (error-active nodes/receivers), the eighth recessive suspend bit (error-passive transmitters), or on leaving bus-integration state. `\newline`{=latex} 7) An error-passive transmitter shall suspend for 8 bit times after intermission. If another node transmits during the suspend window, the node shall become a receiver. |
| REQ-009 | 6.6.7.5 | P1 | **Bus re-integration** `\newline`{=latex} 1) Nodes shall start bus re-integration on protocol startup or bus-off recovery. `\newline`{=latex} 2) Re-integration completes after 11 consecutive recessive bits at the sample point. `\newline`{=latex} 3) For FD-capable nodes, any synchronisation-causing edge shall reset the 11-bit recessive-bit counter. |
| REQ-010 | 6.6.7.3, 6.6.8 | P1 | **Frame initiation** `\newline`{=latex} 1) SOF shall be a single dominant bit. `\newline`{=latex} 2) A node shall send SOF only when the bus is idle. `\newline`{=latex} 3) A dominant third intermission bit causes a node with a pending frame (if error-active or previous receiver) to skip SOF and begin arbitration immediately. `\newline`{=latex} 4) A dominant bit detected during bus idle shall be interpreted as SOF. |
| REQ-011 | 6.6.10.1, 6.6.10.4 | P2 | **Remote frame** `\newline`{=latex} 1) RTR bit shall be dominant for DFs and recessive for RFs. `\newline`{=latex} 2) RFs have no data field. DLC indicates the requested data length. |
| REQ-012 | 6.6.10.2, 6.6.11.2 | P2 | **SRR and RRS reserved bits** `\newline`{=latex} 1) SRR (CE, FE): transmitted recessive. Receivers accept either polarity without form error. `\newline`{=latex} 2) RRS (FB, FE): transmitted dominant. Receivers accept either polarity without form error. |
| REQ-013 | 6.6.10.5, 6.6.11.5 | P1 | **CRC bit stream coverage** `\newline`{=latex} 1) CC: SOF, arbitration, control, and data fields (dynamic stuff bits excluded). `\newline`{=latex} 2) FD: SOF, arbitration, control, data, dynamic stuff bits, and SBC field (fixed stuff bits excluded). |
| REQ-014 | 6.6.10.6, 6.6.11.6, 6.6.14 | P1 | **Frame acknowledgement** `\newline`{=latex} 1) Receivers shall ACK consistent frames and flag inconsistent frames with an EF. `\newline`{=latex} 2) Transmitters shall flag unacknowledged frames with an EF. `\newline`{=latex} 3) FD frames tolerate up to a 2-bit dominant ACK overlap to compensate for receiver phase differences. `\newline`{=latex} 4) The ACK delimiter shall be one recessive bit. The ACK slot is surrounded by recessive bits on both sides. `\newline`{=latex} 5) ACK shall not depend on the frame identifier. |
| REQ-015 | 6.6.10.3, 6.6.11.2, 6.6.11.3 | P1 | **FDF bit and CC/FD format differentiation** `\newline`{=latex} 1) FDF: dominant in CC frames, recessive in FD frames. `\newline`{=latex} 2) The first two CC control field bits (IDE in CBFF, r1/r0 in CEFF) shall be transmitted dominant. `\newline`{=latex} 3) An FD-tolerant receiver that samples a recessive r0 (CBFF) or r1 (CEFF) shall detect a protocol exception event. `\newline`{=latex} 4) In FBFF the IDE bit is the first control field bit. In FEFF it is in the arbitration field after SRR. `\newline`{=latex} 5) The res bit (FB, FE) shall be dominant. A recessive res bit is a form error. |
| REQ-016 | 6.6.11.3 | P2 | **ESI bit transmission** `\newline`{=latex} 1) Recessive when the LLC ESI flag is set. `\newline`{=latex} 2) Dominant when LLC ESI flag is clear and the node is error-active. `\newline`{=latex} 3) Recessive when LLC ESI flag is clear and the node is error-passive. |
| REQ-017 | 6.6.11.5 | P1 | **Stuff count (SBC) field** `\newline`{=latex} The SBC field shall be Gray-coded with a parity bit and placed before the FCRC, encoding the count of dynamic stuff bits in the frame. |
| REQ-018 | 6.6.10.5, 6.6.11.5 | P2 | **CRC delimiter** `\newline`{=latex} 1) CC frames: exactly one recessive CRC delimiter bit. `\newline`{=latex} 2) FD frames: transmitter sends one recessive bit. A second recessive bit is tolerated before the ACK slot. |
| REQ-019 | 6.6.13.1, 6.6.13.2, 6.6.13.3.1 | P1 | **Bit stuffing** `\newline`{=latex} 1) After 5 consecutive identical bits, a complementary dynamic stuff bit shall be inserted. The receiver shall discard it. `\newline`{=latex} 2) CC stuffing covers SOF through the FCRC sequence. FD dynamic stuffing covers SOF through the data field only. `\newline`{=latex} 3) The ACK field and EOF shall not be dynamically stuffed. `\newline`{=latex} 4) FD frames (FB, FE): a fixed stuff bit precedes the SBC field and follows every fourth CRC bit, each the inverse of the preceding bit. If the preceding field ends with 5 consecutive identical bits, only the FSB is emitted. A receiver detecting a fixed stuff bit with the same polarity as the preceding bit shall flag a form error. |
| REQ-020 | 6.6.10.7, 6.6.11.7, 6.6.15.2 | P1 | **End of frame (EOF)** `\newline`{=latex} 1) EOF shall consist of exactly 7 recessive bits. `\newline`{=latex} 2) A receiver shall validate a frame after the sixth EOF bit. `\newline`{=latex} 3) A dominant seventh EOF bit triggers an OF rather than a form error. `\newline`{=latex} 4) A transmitter requires all seven EOF bits to be error-free. |
| REQ-021 | 6.6.10.2, 6.6.11.2, 6.6.17.4, 6.6.17.5 | P1 | **Bus arbitration** `\newline`{=latex} 1) Recessive-sent, dominant-monitored: lose arbitration and become a receiver. `\newline`{=latex} 2) Dominant-sent, recessive-monitored: bit error. `\newline`{=latex} 3) Arbitration spans from the first ID bit to: IDE (CB), RTR (CE), or the bit after FDF (FB/FE). `\newline`{=latex} 4) When CB and FB frames with the same base ID collide, the CB frame wins via its dominant r0/FDF bit. `\newline`{=latex} 5) When a CBFF/FBFF and CEFF/FEFF frame with the same 11-bit base ID collide, the CBFF/FBFF frame wins via the recessive SRR/IDE bits. `\newline`{=latex} 6) When a DF and RF with the same ID and format collide, the DF wins via its dominant RTR bit. |
| REQ-022 | 6.6.21.2 | P1 | **MAC error detection** `\newline`{=latex} 1) Bit error: any sent/monitored mismatch. Exceptions: recessive non-stuff bits during arbitration or ACK slot, and dominant bits monitored while sending a passive error flag. `\newline`{=latex} 2) Stuff error: detected at the sixth consecutive identical bit in any dynamically-stuffed field. `\newline`{=latex} 3) Form error: any unexpected fixed-form bit value (all formats) or a fixed stuff bit not the inverse of the preceding bit (FB, FE). Exception: a dominant bit at the last EOF bit or last delimiter bit is not a form error. `\newline`{=latex} 4) CRC error: FCRC mismatch. FD frames additionally require SBC match and correct SBC parity. `\newline`{=latex} 5) ACK error: transmitter detects no dominant bit in the ACK slot. |
| REQ-023 | 6.6.21.3.1 | P1 | **Error flag initiation and FCRC error behaviour** `\newline`{=latex} 1) On any detected error, start an EF at the immediately following bit and inform the LLC. `\newline`{=latex} 2) Data-phase errors require a switch back to nominal bit rate before the EF starts. `\newline`{=latex} 3) On an FCRC error, the receiver shall withhold ACK. `\newline`{=latex} 4) On an FCRC error, the receiver sends an EF at the first EOF bit (CC) or 3 bit-times after the CRC delimiter (FD). |
| REQ-024 | 6.6.21.3.2 | P2 | **Overload frame** `\newline`{=latex} 1) LLC-requested OF: starts at the first intermission bit. Optional, at most 2 per inter-frame space. `\newline`{=latex} 2) Reactive OF: triggered by a dominant bit at either of the first two intermission bits, the last EOF bit (receivers only), or the last error/overload delimiter bit. Starts one bit after the triggering bit. |
| REQ-025 | 7.3.2, 7.3.3 | P1 | **PCS bit timing configuration** `\newline`{=latex} 1) Separate nominal and FD data bit rate configurations (XL excluded), each based on a programmable integer prescaler. `\newline`{=latex} 2) Sample point shall be at the end of Phase_Seg1. `\newline`{=latex} 3) Sync_Seg shall be exactly 1 tq. `\newline`{=latex} 4) IPT ≤ 2 tq. SJW ≤ min(Phase_Seg1, Phase_Seg2) in both phases. `\newline`{=latex} 5) In FD data bit time, Phase_Seg2 ≥ SJW and ≥ maximum IPT. |
| REQ-026 | 7.3.2 | P2 | **Propagation segment sizing** `\newline`{=latex} prop_seg ≥ t_node(A) + t_node(B) + 2 × t_busline, where each node delay includes transceiver round-trip delay. |
| REQ-027 | 7.3.4, 6.6.21.3.1 | P1 | **Transmitter delay compensation** `\newline`{=latex} 1) FD-capable nodes shall implement TDC for the FD data phase when BRS is recessive, with programmable enable and prescaler constrained to 1 or 2. `\newline`{=latex} 2) SSP position = measured transmitter delay + configured offset. `\newline`{=latex} 3) When TDC is active, bit errors at the regular SP are ignored. Errors at the SSP are reacted upon at the following nominal SP. |
| REQ-028 | 7.3.5.1, 7.3.5.2, 7.3.5.3, 7.3.5.4 | P1 | **PCS synchronisation** `\newline`{=latex} 1) Only one synchronisation per bit time. Re-enabled after the next recessive sample point. `\newline`{=latex} 2) Edges qualify only if the previous sample point was recessive and no positive phase error exists while sending dominant. `\newline`{=latex} 3) Hard synchronisation occurs on IFS edges, during bus-integration, and at the FDF-to-res transition. Correction is unconstrained by SJW and restarts the bit time with Sync_Seg completed. `\newline`{=latex} 4) All other qualifying edges cause resynchronisation, except during the FD data phase. Positive phase error: Phase_Seg1 lengthened by SJW. Negative phase error: Phase_Seg2 shortened by SJW. When error ≤ SJW the effect equals hard synchronisation. |
| REQ-029 | 7.3.6 | P2 | **Oscillator frequency tolerance** `\newline`{=latex} 1) df < SJW / (20 × tbit) and df < min(Phase_Seg1, Phase_Seg2) / (2 × (13 × tbit − Phase_Seg2)). `\newline`{=latex} 2) SJW shall not exceed min(Phase_Seg1, Phase_Seg2). |
| REQ-030 | 6.6.7.5, 7.4.2.2, 7.4.2.3, 8.1.4.4 | P1 | **PCS bus-off isolation and signalling** `\newline`{=latex} 1) During bus-off, the PCS shall not drive dominant bits. `\newline`{=latex} 2) The PCS counts consecutive recessive bits at the sample point and strobes idle_condition after 11, resetting on any dominant. `\newline`{=latex} 3) The PCS shall disconnect the node from the bus on a bus_off_request. `\newline`{=latex} 4) The PCS shall reconnect on a bus_off_release_request. |
| REQ-031 | 8.1.3.2, Table 14, Table 15 | P2 | **FCE reset and response** `\newline`{=latex} 1) Normal_mode_request resets TEC and REC to zero. `\newline`{=latex} 2) FCE shall assert Normal_mode_response to the LLC after servicing a Normal_mode_request. |
| REQ-032 | 8.1.4.2 rule a), rule b), rule c), rule d), rule e), rule f), rule g), rule h) | P1 | **Error counter rules** `\newline`{=latex} 1) REC +1 on any receiver-detected error (except bit errors during error/overload flag). `\newline`{=latex} 2) REC +8 on first dominant bit after sending an error flag. `\newline`{=latex} 3) REC +8 on bit error while sending an active error or overload flag. `\newline`{=latex} 4) REC -1 on successful RX if in 1-127. Stays 0 if already 0. Set to 119-127 if above 127. `\newline`{=latex} 5) TEC +8 when sending an error flag (exceptions: error-passive ACK error, pre-arbitration stuff error). `\newline`{=latex} 6) TEC +8 on bit error while sending an active error or overload flag. `\newline`{=latex} 7) TEC -1 on successful TX (floor 0). `\newline`{=latex} 8) After 14 consecutive dominant bits following an active error or overload flag (8 after a passive error flag), increment both counters by 8. `\newline`{=latex} 9) Further +8 for each additional 8 consecutive dominant bits. |
| REQ-033 | 8.1.4.3, 8.1.4.4, 8.1.3.2 | P1 | **Error confinement state transitions** `\newline`{=latex} 1) Either counter exceeding 127 causes error-passive. `\newline`{=latex} 2) Both counters at or below 127 restores error-active. `\newline`{=latex} 3) TEC exceeding 255 causes bus-off. `\newline`{=latex} 4) Bus-off recovery requires 128 idle conditions; both counters reset to zero. `\newline`{=latex} 5) FCE shall assert Bus_off notification to the LLC on entering bus-off state. |
| REQ-034 | 6.6.11.3, 7.2.5, 7.2.6 | P1 | **BRS bit rate switching** `\newline`{=latex} 1) A recessive BRS bit switches from nominal to FD data rate at the BRS sample point. A dominant BRS bit keeps nominal rate. `\newline`{=latex} 2) The bit rate switches back to nominal at the sample point of the first CRC delimiter bit. |
| REQ-035 | 6.4.3, 6.4.4, Table 5 | P1 | **DLC byte-count mapping** `\newline`{=latex} 1) DLC 0-8 map linearly. FD extends DLC 9→12, 10→16, 11→20, 12→24, 13→32, 14→48, 15→64 bytes. CC treats DLC 9-15 as 8 bytes. `\newline`{=latex} 2) An LLC restricted to fewer bytes shall replace excess bytes with 0xCC padding in both TX and RX directions. |
| REQ-036 | 6.6.16 | P3 | **Bit transmission order** `\newline`{=latex} Within each field the MSB is transmitted first. Within the data field byte 0 is sent first. Within each byte bit(7) is sent first. |
| REQ-037 | 8.1.5 | P2 | **Node start-up** `\newline`{=latex} 1) A node shall complete bus re-integration (11 consecutive recessive bits) before transmitting. `\newline`{=latex} 2) A single node that receives no ACK shall become error-passive but shall never go bus-off. The error-passive ACK exception applies indefinitely. |
| REQ-038 | 6.6.18 | P2 | **MAC data consistency** `\newline`{=latex} Transmitted frames shall be protected against mid-transmission corruption by at least one of: (a) buffering the full frame before TX starts, or (b) LLC data-error checking with frame invalidation on detection. |
| REQ-039 | 6.6.21.1, 6.6.21.4 | P2 | **Error signalling enable** `\newline`{=latex} A configuration parameter shall exist to enable or disable error signalling. §6.6.21.4 defines the full behaviour when error signalling is disabled. |

: ISO 11898-1 extracted requirements. {#tbl:appendix-requirements}
:::
