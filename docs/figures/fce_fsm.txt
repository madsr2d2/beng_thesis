---
config:
  theme: neutral
  layout: elk
  flowchart:
    curve: monotoneX
  elk:
    algorithm: layered
    mergeEdges: false
    nodePlacementStrategy: BRANDES_KOEPF
    edgeRouting: SPLINES
---
flowchart TD

classDef reset fill:#F5C2C0,stroke:#BB3333,stroke-width:4px,color:#000000
classDef ff    fill:#CDFDC5,stroke:#33AA33,color:#000000

s_error_active("`**s_error_active**
─────────
• Normal operation
• mac_o.error_active = 1
• TEC/REC updated per ISO 8.1.4.2`")

s_error_passive("`**s_error_passive**
─────────
• mac_o.error_active = 0
• TEC/REC updated per ISO 8.1.4.2`")

s_bus_off("`**s_bus_off**
─────────
• mac_o.bus_off = 1
• llc_o.bus_off = 1
• pcs_o.bus_off = 1
• Count idle_condition pulses`")

s_error_active -->|"TEC > 127 or REC > 127"| s_error_passive
s_error_active -->|"TEC > 255"| s_bus_off
s_error_passive -->|"TEC ≤ 127 and REC ≤ 127"| s_error_active
s_error_passive -->|"TEC > 255"| s_bus_off
s_bus_off -->|"128 idle conditions (TEC/REC reset to 0)"| s_error_active

class s_error_active reset
class s_error_passive,s_bus_off ff
