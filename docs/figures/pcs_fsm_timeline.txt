---
config:
  theme: neutral
  flowchart:
    curve: monotoneX
---
flowchart LR

classDef reset fill:#F5C2C0,stroke:#BB3333,stroke-width:4px,color:#000000
classDef ff    fill:#CDFDC5,stroke:#33AA33,color:#000000
classDef event fill:#FFF3CD,stroke:#CC9900,stroke-width:2px,color:#000000

s_sync_seg("`**s_sync_seg**
─────────
• 1 TQ fixed
• Hard sync: dominant edge
  restarts bit period here`")

s_prop_seg("`**s_prop_seg**
─────────
• active_prop_seg TQs
• Soft sync detected here:
  phase_seg1 extended by SJW`")

s_phase_seg1("`**s_phase_seg1**
─────────
• active_phase_seg1 TQs
  (+SJW if soft sync)
• SSP fires 1 TQ before end
  (if ssp_active)`")

ev_sp("`**Sample Point**
─────────
• mac_o.sample_point = 1
• rx_data latched
• BRS recessive → data-phase timing
• data_phase_stop → nominal timing`")

s_phase_seg2("`**s_phase_seg2**
─────────
• active_phase_seg2 TQs
  (−SJW if soft sync)
• TDC: tdc_count_active starts
  at FDF res bit boundary`")

ev_bb("`**Bit Boundary**
─────────
• tx_o ← mac_i.tx_data
• mac_o.bit_boundary = 1`")

s_sync_seg -->|"1 TQ"| s_prop_seg
s_prop_seg -->|"seg_count ≥ active_prop_seg − 1"| s_phase_seg1
s_phase_seg1 -->|"seg_count ≥ active_phase_seg1 − 1"| ev_sp
ev_sp --> s_phase_seg2
s_phase_seg2 -->|"seg_count ≥ active_phase_seg2 − 1"| ev_bb
ev_bb -->|"next bit"| s_sync_seg

class s_sync_seg reset
class s_prop_seg,s_phase_seg1,s_phase_seg2 ff
class ev_sp,ev_bb event
