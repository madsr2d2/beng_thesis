---
config:
  theme: neutral
  layout: elk
  flowchart:
    curve: monotoneX
  elk:
    algorithm: layered
    mergeEdges: false
    nodePlacementStrategy: BRANDES_KOEPF
    edgeRouting: SPLINES
---
flowchart LR
    iso_pdf("`PDF`")
    iso_md("`MD`")
    
    norm_set("`**Normative set**
    (Size = 168)
    ─────────
    + source_clause
    + original_wording`")
    
    req_set("`**Requirement set**
    (Size = 38)
    ─────────
    + id
    + paraphrase
    + priority
    + notes
    `")

    ver_plan("`**Verification plan**
    ─────────
    + layer
    + side
    + format
    + observability
    + method
    + status
    + coverage_criteria
    + label
    + file`")

    iso_pdf ==> iso_md ==>|"`Extraction of 
    normative statements by
    LLM agent`"| norm_set ==>|Requirements
    destilation| req_set ==> ver_plan
